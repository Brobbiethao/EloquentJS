/* Excuses for not picking this right back up:
Wife had pneumonia, single parent mode
Kid's picked strep throat
2am Hospital emergency
Overtime at work for projects
3 hour daily two way commutes
The Christmas decor is still up, needs to be taken down
The cars need oil changes, tire pressure checks, spark plug and cabin filter swaps
5 loads of laundy are waiting
Floors need to be swifter dusted
Boxes need to be compressed for recycling
Wedding attended
Volunteered a day at each kid's school (2 days)
*/


#1:
// Your code here.
function min(num1, num2) {
  return Math.min(num1, num2)
}

console.log(min(0, 10));
// → 0
console.log(min(0, -10));
// → -10


#2
// Your code here.
function isEven(num) {
  let n = 0;
  num < 0 ? n+= (num-2) * -1 : n+= num
  if(n % 2 === 0) return true;
  if(n % 2 > 0) return false;
};

console.log(isEven(50));
// → true
console.log(isEven(75));
// → false
console.log(isEven(-1));
// → ??
console.log(isEven(-2));


#3
// Your code here.
function countBs(string) {
  let count = 0;
  for(var i = 0; i <= string.length; i++) {
     if(string[i] === 'B') count++
  };
  return count
}

function countChar(string, letter) {
  let count = 0;
  for(var i = 0; i <= string.length; i++) {
    if(string[i] === letter) count++
  };
  return count;
}
  

console.log(countBs("BBC"));
// → 2
console.log(countChar("kakkerlak", "k"));
// → 4
